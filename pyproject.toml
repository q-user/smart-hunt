[project]
name = "django-jobs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.135.2",
]

[dependency-groups]
main = []
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.8",
]
